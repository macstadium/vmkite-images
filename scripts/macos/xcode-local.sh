#!/bin/bash
set -euxo pipefail

echo "llamas rock"

# if [ ! -f "xcode-cache/${XCODE_VERSION}.tar" ] ; then
#   echo "Failed to find xcode cache"
#   exit 1
# fi

# tar cf "xcode-cache/${XCODE_VERSION}.tar" "/Applications/Xcode-${XCODE_VERSION}.app"